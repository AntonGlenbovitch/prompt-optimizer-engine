[project]
name = "prompt-optimizer-engine"
version = "0.1.0"
description = "Prompt Optimization Engine for LLMs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
prompt-optimizer = "prompt_optimizer.main:main"
